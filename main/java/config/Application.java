package config;

/*
 * Prezados, houve uma extensão de prazo devido a 
 * minha semana um tanto corrida devido a problemas na faculdade e agradeço a compreensao.
 * Mesmo com a extensao de mais tres dias de prazo, eu nao consegui fazer o solicitado.
 * Tinha como objetivo fazer a validacao para logar, na pagina index haver a solicitaçao para traduzir o rss para json
 * e novamente haver a autentificacao. Estou enviando um pouco de cada que consegui fazer em todo horario util que tive.
 * Muito obrigado por tudo!
 *	Atenciosamente, 
 *
 * 
 * 
 * */
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class Application {

    public static void main(String[] args) throws Throwable {
        SpringApplication.run(Application.class, args);
    }

}

